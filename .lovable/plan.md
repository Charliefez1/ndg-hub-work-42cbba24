

## GitHub CI Setup

The user wants to add a GitHub Actions CI workflow. This is straightforward — create the workflow file and ensure the required npm scripts exist in `package.json`.

Let me check what scripts are currently configured.

